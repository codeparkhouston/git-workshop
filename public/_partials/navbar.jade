// Remove the "navbar-fixed-top" class if you don't want it fixed at the top of
// the document.

.navbar.navbar-inverse.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='#{site.url}')= event.title
    #navbar-collapse.navbar-collapse.collapse
      ul.nav.navbar-nav.navbar-right
        li(class="#{ current.path[0] == 'index' ? 'active' : '' }")
          a(href='#{site.url}') About
        li(class="#{ current.path[0] == 'sign-up' ? 'active' : '' }")
          a(href='#{site.url}/sign-up') Sign Up
    // /.navbar-collapse 